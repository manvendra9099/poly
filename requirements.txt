# btcfm runtime requirements
#
# JAX is NOT listed here — it must be installed BEFORE this file.
# On CCuB (Python 3.11 + CUDA 12, no standalone cuDNN module):
#
#   pip install --user "jax[cuda12_pip]" \
#       -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
#
# cuda12_pip bundles cuDNN alongside the jaxlib wheel — no cuDNN module needed.
# Only the GPU driver (libcuda.so) must be present, which is always true on
# GPU compute nodes.
#
# After installing JAX, run:
#   pip install --user -r requirements.txt
#   pip install --user --no-deps -e .

flax>=0.8.0
optax>=0.2.0
numpy>=1.26
polars>=1.40,<2
# <18.0: pyarrow 18+ dropped pre-built cp311 manylinux wheels on this platform;
#         building from source requires Rust (not available on CCuB).
pyarrow>=14.0,<18.0
requests>=2.31
websockets>=12.0
pyyaml>=6.0
pydantic>=2.0
matplotlib>=3.8

# Development / testing
pytest>=8.0
pytest-asyncio>=0.23
